package:
    name: expat
    version: 2.1.0
source:
    fn: expat-2.1.0.tar.gz
    url: http://sourceforge.net/projects/expat/files/expat/2.1.0/expat-2.1.0.tar.gz/download

#requirements:
#  build:
#    - cmake

build:
    number: 1

about:
  home: http://www.libexpat.org/
  license: MIT License
  summary: Expat is an XML parser library written in C. It is a stream-oriented parser in which an application registers handlers for things the parser might find in the XML document (like start tags).